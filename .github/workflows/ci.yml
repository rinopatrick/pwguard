name: CI
on:
  push:
    branches: ["main", "master"]
  pull_request:
jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Repo health
        run: |
          test -f LICENSE
          echo "Basic checks passed"
